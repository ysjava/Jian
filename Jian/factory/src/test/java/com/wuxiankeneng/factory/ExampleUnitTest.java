package com.wuxiankeneng.factory;

import android.util.Log;

import org.junit.Test;

import java.util.Arrays;
import java.util.HashMap;
import java.util.Map;

import static org.junit.Assert.*;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {
        Map<Integer, Integer> map = new HashMap<>();


//        assertEquals(4, 2 + 2);
        // nums = [2, 7, 11, 15], target = 9
//        System.out.println("结果 :" + Arrays.toString(twoSum(new int[]{4,4, 16, 26, 6, 15}, 8)));
    }

    private int[] twoSum(int[] nums, int target) {
        Map<Integer, Integer> map = new HashMap<>();
return null;
//        for (int i = 0; i < 3; i++) {
//            map.put(i, i);
//        }


//        for (int i = 0; i < nums.length; i++) {
//            int del = target - nums[i];
//            if (map.containsKey(del) && map.get(del) != i) {
//                return new int[]{i, map.get(del)};
//            }
//        }
//        return null;
//        Map<Integer, Integer> map = new HashMap<>();
//        for (int i = 0; i < nums.length; i++) {
//            int del = target - nums[i];
//            if (map.containsKey(del)) {
//                return new int[]{map.get(del), i};
//            }
//            map.put(nums[i], i);
//        }
//        return null;
//    }
    }
}